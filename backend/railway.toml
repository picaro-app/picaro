[build]
builder = "NIXPACKS"

[deploy]
startCommand = "python -m uvicorn ai_server:app --host 0.0.0.0 --port $PORT"

[phases.setup]
nixPkgs = ["glib", "libGL"]

[phases.install]
cmds = ["pip install -r requirements.txt"]