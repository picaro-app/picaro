[build]
builder = "NIXPACKS"

[deploy]
startCommand = "python3 -m uvicorn ai_server:app --host 0.0.0.0 --port $PORT"

[phases.setup]
nixPkgs = ["python311", "glib", "libGL", "xorg.libxcb"]

[phases.install]
cmds = ["pip install -r requirements.txt"]